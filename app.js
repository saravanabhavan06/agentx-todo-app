(function(){const input=document.getElementById('task-input'),addBtn=document.getElementById('add-btn'),list=document.getElementById('task-list'),count=document.getElementById('count'),clearBtn=document.getElementById('clear-btn');let tasks=JSON.parse(localStorage.getItem('agentx-tasks')||'[]');function save(){localStorage.setItem('agentx-tasks',JSON.stringify(tasks))}function updateCount(){const r=tasks.filter(t=>!t.done).length;count.textContent=r+' task'+(r!==1?'s':'')+' remaining'}function render(){list.innerHTML='';tasks.forEach((task,i)=>{const li=document.createElement('li');if(task.done)li.classList.add('done');const cb=document.createElement('input');cb.type='checkbox';cb.checked=task.done;cb.addEventListener('change',()=>{tasks[i].done=cb.checked;save();render()});const span=document.createElement('span');span.textContent=task.text;const del=document.createElement('button');del.className='del';del.textContent='x';del.addEventListener('click',()=>{tasks.splice(i,1);save();render()});li.append(cb,span,del);list.appendChild(li)});updateCount()}function addTask(){const text=input.value.trim();if(!text)return;tasks.push({text,done:false});input.value='';save();render()}addBtn.addEventListener('click',addTask);input.addEventListener('keydown',e=>{if(e.key==='Enter')addTask()});clearBtn.addEventListener('click',()=>{tasks=tasks.filter(t=>!t.done);save();render()});render()})();